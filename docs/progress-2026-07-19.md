# Cloudflare Deployment Handoff

Snapshot time: 2026-07-19 05:57 UTC
Addendum: 2026-07-19 (config-standard alignment, local only)

## Addendum: config-standard alignment (uncommitted)

The repository was audited against "GitHub 配置标准 v1.0" and the following
local changes were made. None of them touch Cloudflare; the deployment stop
point below is unchanged.

- `src/value-normalizer.mjs`: separator characters (`-`, `_`, `.`, `/`) are
  now unified into spaces per §2.4; new `test/value-normalizer.test.mjs`.
- `scripts/validate-config.mjs`: added checks for the five fixed categories,
  duplicate `display_name_zh_cn`, regex-alias `test_cases` execution, and
  canonical JSON formatting (§13 items 10, 12, 13, 16, 18).
- `scripts/check-version-monotonic.mjs` (new, `npm run check:versions`):
  compares `config/*.json` against `origin/main`; content changes require a
  version bump and versions must not regress (§13 item 17).
- `.github/workflows/validate-config.yml` replaces `config-check.yml`
  (standard file name, Node 22, `fetch-depth: 0`, split steps).
- New docs: `CONFIG_GUIDE.md`, `REVIEW_GUIDE.md`, `NAMING_GUIDE.md`,
  `CHANGELOG.md`; README updated accordingly.
- Config release 1.2.0: `tag_uncensored` / `tag_censored` deprecated per
  standard §5 (censorship state is metadata, not a tag); their spelling
  variants added to `ignored.json` as `ignore_000010`–`ignore_000016`.
- `npm run check` passes: 37 tests, 9 configs validated, versions monotonic,
  manifest and Worker dry-run build succeed.

## Repository state

- Branch: `main`
- Commit: `dbfe1b2` (`feat: add D1 media ingest worker`)
- Remote state: `main` matched `origin/main` before this handoff note.
- `wrangler.jsonc` still contains the placeholder D1 database ID:
  `00000000-0000-0000-0000-000000000000`.
- `.dev.vars` contains the local `INGEST_TOKEN` variable. Its value is not
  recorded here or included in the backup.
- `.gitignore` has uncommitted additions for `.env` and `.backup/`.

## Completed

- Cloudflare Wrangler OAuth login completed and `wrangler whoami` succeeded.
- Account email: `niu900326@gmail.com`
- Cloudflare account ID: `f117c6ad8cfd194f0a85610274d8978d`
- `npm run check` passed:
  - 9 configuration files validated against 9 schemas.
  - 194 active aliases checked with no conflicts.
  - 31 tests passed.
  - Config manifest and Worker dry-run build succeeded.
- Existing D1 databases were inspected:
  - `bn2`: `be91307e-7074-4df6-80fe-5c731671fb34`
  - `boringniu-media`: `3ff065ee-2b6e-4ddd-8534-90455cda80a7`
- No existing database is named `bn-media`.

## Current stop point

Creation of the new `bn-media` D1 database was proposed, but the approval
request was cancelled. No database was created and no existing Cloudflare
resource was modified.

The following deployment operations have not been performed:

- Create or select the production D1 database.
- Replace the placeholder `database_id` in `wrangler.jsonc`.
- Apply D1 migrations remotely.
- Set the remote `INGEST_TOKEN` Worker secret.
- Deploy the `bn-api` Worker.
- Verify the deployed `/health` endpoint.

## Resume procedure

1. Verify or renew Wrangler authentication:

   ```bash
   XDG_CONFIG_HOME=/tmp/boringniu-wrangler npx wrangler whoami
   ```

2. Decide whether to create the intended `bn-media` database or deliberately
   reuse one of the existing empty databases. Creating `bn-media` matches the
   repository configuration and README.

3. If creating it, run:

   ```bash
   XDG_CONFIG_HOME=/tmp/boringniu-wrangler npm run db:create
   ```

4. Write the returned database UUID into `wrangler.jsonc`.

5. Apply the migrations:

   ```bash
   XDG_CONFIG_HOME=/tmp/boringniu-wrangler npm run db:migrate:remote
   ```

6. Set the Worker secret without printing its value:

   ```bash
   XDG_CONFIG_HOME=/tmp/boringniu-wrangler npx wrangler secret put INGEST_TOKEN
   ```

7. Deploy and verify:

   ```bash
   XDG_CONFIG_HOME=/tmp/boringniu-wrangler npm run deploy
   ```

   Then request `GET /health` from the deployed Worker URL.

## Credential note

Wrangler OAuth credentials are currently under
`/tmp/boringniu-wrangler/.wrangler/config/default.toml`. That location is
temporary and is intentionally excluded from the backup. A new OAuth login may
be required after the development environment restarts.
